echo "# data-extract-loading" >> README.md

git init

git add README.md

git commit -m "Initial repository"

git branch -M master

git remote add origin git@github.com:hanwell2002/data-extract-loading.git

git push -u origin master

